import 'package:flutter/material.dart';

class CloudMessaging extends StatelessWidget {
  bool hideBody = false;
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
      'Push Notificaiton',
      style: TextStyle(fontSize: 26),
    ));
  }
}
